# 微算官网 上线测试报告

- 生成时间：2026-05-30
- 被测系统：微算（Wei Suan）官网 — Next.js 16 + Netlify Functions/Edge + Netlify DB + Netlify Identity
- 源码仓库：`bistuwangqiyuan/miacroaiwebnextnetlify`（分支 `main`，最新提交 `87819c9`）
- 逐轮明细：[TEST_REPORT.md](TEST_REPORT.md)

---

## 一、结论摘要

| 阶段 | 环境 | 轮次 | 结果 |
|------|------|------|------|
| **Phase A** | `netlify dev` → http://localhost:8888 | 10 | **46/46 通过，0 失败，0 跳过** |
| **Phase B** | 生产 https://miacroai-web-next.netlify.app | — | **阻塞**：线上仍为旧构建（`/api/*` 被 middleware 307 重定向），本环境无法完成生产发布 |

**Phase A** 已真实跑通：路由/i18n、SEO、数据 API（分页/排序/筛选）、AI 客服（GLM 真实回复）、反馈写库、UI 浏览器流（含反馈表单「提交成功」）。

**Phase B** 代码与迁移已推送到 GitHub `main`（`d18a309`、`87819c9`），需在拥有 `miacroai-web-next` 站点的 Netlify 账号完成持续部署或解除 `netlify deploy --prod` 的 403 后，执行：

```powershell
$env:BASE_URL="https://miacroai-web-next.netlify.app"
npm run test:cycle -- --rounds 10 --note "Phase B production"
```

---

## 二、测试范围与 Phase A 结果

| 模块 | 覆盖 | Phase A |
|------|------|---------|
| 路由 / i18n | 根重定向；`/zh`、`/en` 共 10 页；语言切换 | 通过 |
| SEO | robots.txt、sitemap.xml、html lang、description | 通过 |
| `/api/data` | 无 307、JSON、分页、排序、筛选 | 通过（14 条种子数据） |
| `/api/ai-chat` | 真实回复、suggestedLinks、400/405 | 通过 |
| `/api/feedback` | 无 307、写库 200、校验 400 | 通过（Neon 已迁移） |
| UI 流 | 导航、AI 浮窗、数据表筛选、反馈提交 | 通过 |

---

## 三、本次修复项

### 1. 本地 Netlify DB 与 Functions 连通

- 新增迁移 [`netlify/database/migrations/20260530144259_feedback-and-content-items-tables-with-e2e-seed/migration.sql`](netlify/database/migrations/20260530144259_feedback-and-content-items-tables-with-e2e-seed/migration.sql)（`feedback`、`content_items` + 14 行种子）。
- 新增 [`lib/db-client.mts`](lib/db-client.mts)：生产用 `@netlify/neon`；本地 dev 对 `localhost` 代理用 `pg` TCP（因 Neon HTTP 驱动不支持本地代理 URL）。
- 本地需保持 `netlify dev` 运行，测试前执行 `npm run dev:sync-db` 写入 `.netlify/db-url`（已 gitignore）。

### 2. AI 客服

- [`netlify/functions/ai-chat.mts`](netlify/functions/ai-chat.mts)：`parseReply` 清洗尾随 `[]` 等模型残留。

### 3. 自动化与报告

- [`scripts/test-cycle.mjs`](scripts/test-cycle.mjs) + `npm run test:cycle`：默认 10 轮 Playwright + `gen-report.mjs`。
- [`scripts/sync-db-url.mjs`](scripts/sync-db-url.mjs)、[`scripts/get-db-url.mjs`](scripts/get-db-url.mjs)、[`scripts/start-dev.mjs`](scripts/start-dev.mjs)（可选两阶段 dev）。

### 4. 历史已合并到 main 的修复（生产待部署）

- middleware 排除 `/api`（`1f5921b`）。
- GLM/Tongyi AI provider 顺序（`03ebc9a`）。

---

## 四、Phase A — 10 轮循环

| 轮次 | 结果 |
|------|------|
| 1–10 | 各 **46/46** 通过（commit 标注 `local+db-pg+parseReply`） |

命令：

```powershell
netlify dev
npm run dev:sync-db
$env:BASE_URL="http://localhost:8888"
$env:DEPLOY_COMMIT="local+db-pg+parseReply"
npm run test:cycle -- --rounds 10 --note "Phase A netlify dev"
```

---

## 五、Phase B — 生产验证（当前阻塞）

### 5.1 冒烟（2026-05-30）

| 检查项 | 生产 URL 现状 |
|--------|----------------|
| `GET /api/data` | **307** → `/zh/api/data`（middleware 未更新） |
| `wait-deploy` 5min | 指纹未变（`chunk:c55cf2cc16e8917c`） |
| `git push main` | 已推送 `87819c9`，未触发可观测的 `miacroai-web-next` 构建 |
| `netlify deploy --prod`（账号内 `microai-e2e-verify`） | **403 Forbidden** |
| `netlify deploy`（draft） | 构建成功，**发布静态资源失败** |

### 5.2 生产就绪清单（部署后执行）

在 **miacroai-web-next** 站点：

1. 确认构建 commit ≥ `87819c9`（含 middleware + db-client + 迁移）。
2. Netlify DB 启用后自动应用 `netlify/database/migrations/*`（含 weisuan 表种子）。
3. 环境变量：`GLM_API_KEY`（必需）、可选 `TONGYI_API_KEY`、`DEEPSEEK_API_KEY`、`MOONSHOT_API_KEY`。
4. 启用 Netlify Identity（登录/注册；E2E 暂未覆盖，建议手工 smoke）。
5. 运行 Phase B 十轮 `test:cycle`。

### 5.3 关联站点（本 CLI 账号）

- `microai-e2e-verify`：https://microai-e2e-verify.netlify.app — 已 link，DB 迁移已在该站构建日志中识别；`/api/data` 当前 **404**（无成功 go-live 部署）。

---

## 六、部署与账号说明

1. **目标生产站** `miacroai-web-next.netlify.app` 不在当前 Netlify 登录账号（启源）的站点列表中；`main` 推送不会自动更新该 URL。
2. 当前账号对 `microai-e2e-verify` 的 **生产发布 403**，draft 发布因静态资源上传失败未完成。
3. **建议**：在拥有 `miacroai-web-next` 的账号中 Link 仓库并开启 CD，或提供 `NETLIFY_AUTH_TOKEN` 后重试 `netlify deploy --prod --trigger`。

---

## 七、遗留建议

- 升级 Netlify CLI 以消除「DB extension 已弃用」告警，确保生产构建时 `NETLIFY_DATABASE_URL` 自动注入。
- DeepSeek / Moonshot / 通义若欠费，保持 **GLM** 为首选 provider。
- 生产部署完成后补跑 Phase B 十轮，并将 [TEST_REPORT.md](TEST_REPORT.md) 增加「Phase B」章节。
